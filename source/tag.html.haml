---
pageable: true
per_page: 99
---
%h1
  Kategorie #{tagname.split('/').last.capitalize}
%ul.list-articles.big
  - page_articles.each_with_index do |article, i|
    %li
      %time= article.date.strftime('%e. %_m. %Y')
      = link_to article.title, article

- if paginate && num_pages > 1
  .paginate.out
    .page-previous
      - if prev_page
        = link_to '‹ Předchozí strana', prev_page
    .page-number
      %span.label.color-red
        = "#{page_number} / #{num_pages}"
    .page-next
      - if next_page
        = link_to 'Další strana ›', next_page
